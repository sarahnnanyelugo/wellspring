import { Link, NavLink } from "react-router-dom";

export const YellowAppButton = (props) => {
  return (
    <>
      <Link to={button}>
        <button>{text}</button>
      </Link>
    </>
  );
};
